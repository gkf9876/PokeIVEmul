<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nature">
	<select id="selectList" parameterType="com.kdw.pokeivemul.Nature.vo.NatureVo" resultType="com.kdw.pokeivemul.Nature.vo.NatureVo">
		/* nature.selectList */
		SELECT
			SEQ
			, NAME
			, H
			, A
			, B
			, C
			, D
			, S
		FROM
			TB_NATURE
		ORDER BY
			SEQ DESC
    </select>
</mapper>